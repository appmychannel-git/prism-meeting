# Server-side STT PoC dependencies. Python 3.9+ recommended.
# If imports fail due to version mismatch, bump the pins below.
livekit>=0.18.0
livekit-api>=0.7.0
azure-cognitiveservices-speech>=1.40.0
python-dotenv>=1.0.0
