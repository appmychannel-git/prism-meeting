# Prism Meeting — 추가기능 후보 (Roadmap)

> 현재 버전에서 회의 기본기(입장·비공개방·방장 종료·초대링크/딥링크/QR·갤러리/발표뷰·채팅·
> 화면공유(+오디오+음질)·뒤로가기 종료)는 완료. 아래는 다음 작업 후보와 실현 가능성 평가.
> 작성 시점: 2026-07-24.

## 후보 목록 (가능 여부 / 난이도 / 필요 조건)

| # | 기능 | 가능? | 클라만/서버 | 난이도 | 필요한 것 |
|---|---|---|---|---|---|
| 1 | 다국어(영어) UI | 가능 | 클라이언트만 | 중하 | `flutter_localizations`+`intl`, 한국어 문자열 전수 추출(ARB), 로케일 자동/언어선택 |
| 2 | 방장 참가자 관리 (음소거·내보내기·전체음소거) | 가능 | 토큰서버 확장 | 중 | LiveKit 서버 API(`mutePublishedTrack`/`removeParticipant`), 토큰서버 엔드포인트 추가(`/end` 패턴 확장), 방장 권한 검증 |
| 3 | 연결 품질 표시 | 가능 | 클라이언트만 | 하 | LiveKit `participant.connectionQuality` 이벤트 → 타일 아이콘 |
| 4 | 회의 녹화 (Zoom식) | 가능 | 서버 인프라 필수 | 상 | LiveKit Egress + 저장소(S3 등) + 과금. 방장 버튼→토큰서버가 Egress 호출 |
| 5 | 회원 로그인 (회원만 방 생성, 비회원 참여만) | 가능 | 백엔드(인증+DB) 필수 | 상 | 사용자 DB, 인증(이메일/소셜), 토큰서버가 방 생성 시 회원 검증 |
| 6 | 채팅 번역 | 가능 | API키(또는 온디바이스 무료) | 중 | 번역엔진: 온디바이스 `google_mlkit_translation`(무료·오프라인·웹미지원) 또는 클라우드 API(Google/Papago 등, 웹지원·과금). 각자 선호언어로 클라이언트 번역(원문+번역 표시). 미디어서버와 무관 |
| 7 | 음성 실시간 번역(자막) | 가능 | 서버(에이전트+STT+번역) 필수 | 상 | STT→번역→자막 파이프라인. LiveKit Agents(에이전트가 오디오 구독→STT(Deepgram/Whisper)+번역→transcription 발행). STT/번역 API 과금. TTS 더빙은 v1 제외(자막이 표준). 미디어서버 결합도 높음 |
| 8 | USB 웹캠(UVC) — 카메라 없는 TV용 | 부분완료 | (노출 경우)클라만 / (미노출)네이티브 | 하~상 | **Camera2가 외장카메라로 노출하는 경우는 구현됨(v1.1.0)**: 카메라 열거·선택메뉴·핫플러그갱신·내장없으면 USB 자동선택. ROM 미노출 시 네이티브 통합 별도(아래 참고) |

## 코멘트
- **1 다국어**: 순수 클라이언트 → 서버 전환과 무관하게 안전. 타깃 해외(르완다·카자흐스탄)라 체감 우선순위 최상. 문자열 양 적어 빠름. (르완다=프랑스어, 카자흐=러시아어 추가 고려)
- **2 방장 관리**: 실사용 유용. 토큰서버 엔드포인트 몇 개 추가로 구현. 자체 서버 전환해도 유지.
- **3 연결 품질**: "꼭 필요"까진 아님. 비용 거의 없음(아이콘 1개) → 2번 작업 시 곁다리로 함께 추천. 단독 우선순위 낮음.
- **4 녹화**: 서버·스토리지·과금 얽힘 → 데모 단계엔 과함. 자체 미디어 서버 전환 시 함께. 기기 로컬녹화는 별개로 복잡, 비권장.
- **5 로그인**: 제품화 단계 기능. 정식 회원제 전이라면 "방 생성만 비번/API키로 잠그는" 경량 게이트로 선행 가능.
- **6 채팅 번역**: 텍스트라 붙이기 쉬움. 미디어서버와 독립 → 지금 붙여도 안전. 웹까지면 클라우드 API, 안드/iOS만이면 무료 온디바이스. ⚠️ DeepL은 키냐르완다·카자흐어 미지원 → 구글 등 지원 provider 선택.
- **7 음성 번역(자막)**: 난이도 상. LiveKit Agents 서버 파이프라인 + STT/번역 과금. 미디어서버 전환과 함께 결정. ⚠️ STT가 카자흐어는 일부, **키냐르완다는 지원 매우 제한적** → 대상 언어로 먼저 검증 필수.

## UVC(USB 웹캠) 상세 — "현재 되나? 개발량은?"
- **✅ v1.1.0에서 "Camera2 노출 경우" 지원 구현됨**: `Hardware.videoInputs()` 로 카메라 열거 →
  앱바 카메라 선택 메뉴(2개 이상일 때 노출) → `LocalVideoTrack.switchCamera(deviceId)` 로 전환.
  내장 카메라 켜기 실패 시 외장(USB) 자동 선택(`_tryEnableAnyCamera`+`_preferExternal`),
  USB 핫플러그(`Hardware.onDeviceChange`)로 목록 자동 갱신. → **ROM이 UVC를 Camera2 external 로
  노출하는 기기에선 이제 USB 웹캠 사용 가능.**
- **핵심 판단(먼저 할 일)**: 실제 타깃 TV에 UVC 캠 꽂고 Camera2/getUserMedia 가 external 카메라로 잡히는지 테스트.
  - 잡히면 → **이미 됨**(선택 메뉴에서 USB 카메라 선택 or 자동).
  - 안 잡히면 → 아래 네이티브 통합 필요(난이도 상, 미구현).
- **네이티브 통합(ROM 미지원 시)**: USB Host API + libuvc/UVCCamera(`AndroidUSBCamera` 등)로 USB에서 직접 프레임 취득 → **커스텀 WebRTC VideoCapturer 로 I420 프레임을 LiveKit 트랙에 주입**하는 네이티브 플러그인 필요. USB 권한/핫플러그, MJPEG/YUYV→I420 변환, 저사양 TV 성능까지 고려. flutter_webrtc 기본 제공 아님 → **상당한 네이티브 작업.** (참고앱 feelspic이 UVC 지원했으나 순수 네이티브 WebRTC라 자체 통합했던 것)
- 곧 도착할 타깃 기기(TV + 회전형 태블릿TV)에서 위 "인식 여부 테스트"부터 하는 걸 권장.

## 그 외 이전 후보 (참고)
- 호출/수신 벨 (FCM 푸시) — 방 밖 사람 부르기. 회의 제품 완성도에 기여, FCM+토큰서버 확장 필요.
- TV D-pad UX(발표자뷰 썸네일 포커스), 12명↑ 페이지네이션, 손들기/이모지 반응, 화면공유 포인터/주석.
- Krisp 노이즈캔슬 — 자체 서버 전환 시 함께(트랙 프로세서).

## 추천 진행 순서
1) 다국어(영어) UI → 2) 방장 관리 (+연결품질 곁다리) → 6) 채팅 번역 → 5) 로그인 / 4) 녹화 / 7) 음성번역 (자체 서버 전환과 묶어서). UVC(8)는 타깃 TV 도착 후 인식 테스트로 분기.
