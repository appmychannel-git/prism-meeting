<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== 화상회의 필수 권한 ===== -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <!-- 화면공유(발표): MediaProjection 캡처는 mediaProjection 타입 포그라운드 서비스가 필요 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
    <!-- Android 13+: 포그라운드 서비스 알림 표시 권한(화면공유 알림) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- ===== 안드로이드TV/디스플레이/태블릿에도 설치되도록 하드웨어 요구를 선택으로 ===== -->
    <!-- TV 박스는 카메라/터치가 없을 수 있으므로 required=false (USB 웹캠 사용 시나리오) -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>
    <uses-feature android:name="android.software.leanback" android:required="false"/>

    <application
        android:label="Prism Meeting"
        android:name="${applicationName}"
        android:banner="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <!-- 안드로이드TV 런처에도 앱 아이콘이 뜨도록 -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
            <!-- App Links: 초대 링크(https://androidtv.mychannel.co.kr/meeting...) 클릭 시 이 앱으로 열기.
                 도메인의 /.well-known/assetlinks.json 로 소유권 검증(autoVerify). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="androidtv.mychannel.co.kr"
                      android:pathPrefix="/meeting"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- 화면공유용 포그라운드 서비스(flutter_background).
             화면 캡처(MediaProjection)를 유지하려면 mediaProjection 타입 FGS가 떠 있어야 한다.
             안드로이드TV는 발표(송출)를 쓰지 않으므로 표시 위주. -->
        <service
            android:name="de.julianassmann.flutter_background.IsolateHolderService"
            android:foregroundServiceType="mediaProjection"
            android:exported="false" />
        <!-- 저가 안드로이드TV/박스(예: MediaTek)는 Vulkan(Impeller) 드라이버가 부실해
             영상 텍스처 할당 실패/프리즈가 발생한다. 호환성 우선으로 Impeller를 끄고
             안정적인 Skia(OpenGL) 렌더러를 사용한다. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
